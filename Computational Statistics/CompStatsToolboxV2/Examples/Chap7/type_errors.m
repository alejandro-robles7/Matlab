alpha = 0.05;
M = 454;
cv = csquantials(Tm,alpha);
pval = length(find(Tm <=Tobs)/M;
for i = 1:M
xs = sigma*rand(1,n) + 454;
Zm(i) = (mean(sxs) - 454)/sigxbar;
end

type1 = length(find(Zm<cv))/M

mu = 445:460;
